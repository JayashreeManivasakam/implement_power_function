def pow(a,b):
    if b==0:
        return 1
    if a==0:
        return 0
    if b==1:
        return a
    p=pow(a,b//2)
    if a%2==0:
        return p*p
    else:
        return p*p*a
ans=pow(a,b)
print(ans%c)
